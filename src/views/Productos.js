import ListaCards from "../components/ListaCards";

const Productos = () => {
    return (
      <div>
        <ListaCards limit={30}/>
      </div>
    );
}
  
export default Productos;
  