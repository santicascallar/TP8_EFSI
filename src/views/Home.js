import Carousel from "../components/Carousel";
import ListaCards from "../components/ListaCards";

const Home = () => {
    return (
      <div>
        <Carousel/>
        <ListaCards/>
      </div>
    );
}
  
export default Home;
  